<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal do app -->
    <style name="Theme.KanbanTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cor de fundo padrão para todas as telas -->
        <item name="android:windowBackground">@color/color_default</item>
        <!-- Cores principais -->
        <item name="colorPrimary">@color/color_default</item>
        <item name="colorPrimaryVariant">@color/color_variant</item>
    </style>
    <style name="CustomTablayout" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="MyRadioButton" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/green</item>
    </style>


</resources>
